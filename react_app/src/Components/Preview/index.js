import { PreviewBox } from "../Styled/PreviewSection"

export const PreviewSection = ({ jsxComponent }) => {
    return (
        <PreviewBox>
            {jsxComponent}
        </PreviewBox>
    )
};
