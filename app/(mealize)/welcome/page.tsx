import { MealizeWelcome } from "@/components/mealize/mealize-welcome";

/** Legacy `/welcome` — marketing + sign-in CTAs; shell from `(mealize)/layout`. */
export default function WelcomePage() {
  return <MealizeWelcome />;
}
